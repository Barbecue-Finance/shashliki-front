export enum Paths {
  empty = '',
  auth = 'auth',
  register = 'register',
  groups = 'groups',
  profile = 'profile',
  createGroup = 'groups/create',
  createOperation = 'groups/create-operation',
  undefinedById = 'groups/:id',
  groupById = 'groups/:id/group',
  eventById = 'groups/:id/event',
  groupByIdReport = 'groups/:id/report'
}
